/*Design a validation system for username using a C program. The username should always start with an alphabet and it can be followed by numbers or alphabets.
Username shall not contain spaces or special characters except underscore.  Display relevant messages if the input string violate the validation rules. 
Example: “Username cant start with a number”, “Username cant have spaces”, “Usernames cant have special characters”, etc.,
If the username is valid, display "Username accepted". Use pointers for accessing strings. Allocate memory dynamically for storing strings*/


#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

int main()
{
    char s[100];
    fgets(s, 100, stdin);
    s[strlen(s)] = '\0';
    
    char *p = (char *)calloc(strlen(s), sizeof(char));
    for (int i=0; s[i]!='\0'; i++) *(p + i) = *(s + i);
    
    if (isdigit(*(p)))
    {
        printf("Username cant start with a number");
        return 0;
    }
    for (int i=0; i<strlen(s); i++)
    {
        if (isalpha(*(p+i)));
        else if (isdigit(*(p+i)));
        else if (*(p+i)=='_');
        else if (isspace(*(p+i)))
        {
            printf("Username cant contain spaces");
            return 0;
        }
        else 
        {
            printf("Username cant contain special characters");
            return 0;
        }
    }
    printf("Username accepted");
    
    return 0;
}
