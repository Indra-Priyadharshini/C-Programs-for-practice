#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, sam=0;
    scanf("%d", &n);
    int *p = (int *)calloc(n, sizeof(int));
    
    for (int i=0; i<n; i++) scanf("%d", p+i);
    
    for (int i=0; i<n; i++) sam+= *(p+i);
    
    printf("%d", sam);
    return 0;
}
