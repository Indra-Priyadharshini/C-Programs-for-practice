/*Implement ‘The Random Number’ game using a C program. Two users are given ‘n’ chances to call out a random number. The initial score for both the user is 500. 
If the random number is a prime, their score is doubled. 
If it is not a prime, their score gets subtracted by 50. Print the final score of both the users. Hint: Read ‘n’ random values from each user)*/
#include <stdio.h>
#include <math.h>

int isprime(int n)
{
    if (n==1) return 0;
    else
        for (int i=2; i<floor(pow(n, 0.5))+1; i++) 
            if (n%i==0) return 0;
    return 1;
}

int main()
{
    int n;
    scanf("%d", &n);
    int num1 = 500, num2 = 500;
    int arr1[n], arr2[n];
    
    //for (int i=0; i<n; i++) scanf("%d", arr1 + i);
    //for (int i=0; i<n; i++) scanf("%d", arr2 + i);
    
    for (int i=0; i<n; i++) 
    {
        int ele;
        scanf("%d", &ele);
        if (isprime(ele)) num1*=2;
        else num1-=50;
    }
    
    for (int i=0; i<n; i++) 
    {
        int ele;
        scanf("%d", &ele);
        if (isprime(ele)) num2*=2;
        else num2-=50;
    }
    
    printf("%d\n%d", num1, num2);
    return 0;
}
