#include <stdio.h>
#include<string.h>
int main(void) {
	// your code goes here
	char s[30],c;
	gets(s);
	int count[256]={0},max=0;
	for(int i=0;s[i]!='\0';i++)
	{
	    count[s[i]]++;
	
	if(max<count[s[i]])
	{
	    max=count[s[i]];
	    c=s[i];
	}
	}
	printf("\nc=%c",c);
	return 0;
	
}
