#include <stdio.h>
#include <string.h>
#include <stdbool.h>

int main()
{
    char s[100];
    fgets(s, 100, stdin);
    int size = strlen(s), count[257]={0};
    
    for (int i=0; i<size; i++) count[(int)s[i]]++;
    
    for (int i=0; i<size; i++) if (count[(int)s[i]]==1) printf("%c\n", s[i]);
    
    return 0;
}
